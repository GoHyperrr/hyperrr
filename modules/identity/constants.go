package identity

// Workflow task names.
const (
	TaskValidateActor = "identity.validate_actor"
)

// Event types.
const (
	EventUserCreated = "identity.user_created"
)

// Map keys.
const (
	KeyID      = "id"
	KeyType    = "type"
	KeyActorID = "actor_id"
	KeyUserID  = "user_id"
	KeyEmail   = "email"
	KeyName    = "name"
)
